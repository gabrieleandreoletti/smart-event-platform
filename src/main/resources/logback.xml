<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <property name="LOG_PATH" value="src/main/resources/logs"/>
    <property name="LOG_FILE" value="${LOG_PATH}/app.log"/>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/application.%d{dd-MM-yyyy}.log</fileNamePattern>
            <maxHistory>7</maxHistory> <!-- conserva ultimi 7 giorni -->
        </rollingPolicy>

        <file>${LOG_FILE}</file>
        <append>false</append> <!-- sovrascrive il file ad ogni avvio -->
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{dd-MM-yyyy HH:mm:ss} - %msg%n</pattern>
        </encoder>
    </appender>


    <logger name="com.sourcesense" level="DEBUG"/>

    <root level="INFO">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>